# App Factory の環境設定
# install.sh が ~/.config/app-factory/config.env に配置する（無ければこのテンプレートから生成）。
# 全ての cron ジョブとスキルはこのファイルを最初に読む。値は自分の環境に合わせて編集すること。
# シークレット（webhook URL・API キー）はここではなく $APP_FACTORY_HOME/.env に置く。

# ジョブ実行環境（.env / data/ / logs/ を置くディレクトリ）
APP_FACTORY_HOME="$HOME/dev/business/claude-cron"

# PRD・portfolio.yml・showcase を置くリポジトリ（private 推奨）
PRD_VAULT_DIR="$HOME/dev/business/prd-vault"

# アプリリポジトリの GitHub owner
GITHUB_OWNER="KojoBarbie"

# 新規アプリの Bundle ID プレフィックス
BUNDLE_ID_PREFIX="com.kojobarbie"

# 新規アプリ（Xcode プロジェクト）を生成するディレクトリ
APPS_DIR="$HOME/dev/business"

# claude CLI のパス（launchd から PATH が引けないため明示）
CLAUDE_BIN="$HOME/.nodebrew/current/bin/claude"

# 人間の判断キューの上限（分）。open PR の判断ブロック（judge:v1）の cost 合計がこれに達すると
# factory-build は新規着手を止める。捌けば自動的に再開する（WIP 制限）。
# 本数ではなく時間で測るのは、10秒のスクショ確認と10分の意思決定を同じ「1本」にしないため。
JUDGE_BUDGET_MINUTES=45

# 判断ブロックの expires を何日後にするか（時効ジョブ run_pr_expiry.sh が参照）
JUDGE_EXPIRY_DAYS=14

# 判断キューのダッシュボード（pr-desk）の URL。Slack 通知の導線に使う
PR_DESK_URL="http://localhost:4321"

# TestFlight 内部テスト配信（app-kickoff が各アプリに用意する内部グループ）
# グループは hasAccessToAllBuilds=true で作られ、INTERNAL_ONLY ビルドが自動配信される。
# TESTFLIGHT_TESTER_EMAILS は既存の App Store Connect ユーザー（チームメンバー）の email をカンマ区切りで。
# 空にするとグループだけ作りテスターは追加しない（後から TestFlight で手動追加）。
TESTFLIGHT_INTERNAL_GROUP="developer"
TESTFLIGHT_TESTER_EMAILS=""
